/*  
1.JavaScript is a powerful programming language that enables web developers to change the content on a website based on events, change a website’s style, and even manage data and provide feedback to users.

2.Browsers mainly use JavaScript to make web pages dynamic.

3.JavaScript is an interpreted language, which means that while loading a web page, the browser reads the JavaScript you write and then runs each line one at a time.  

4. Create the event listener 

First, create an element with an onclick attribute. This attribute is the event listener for the event, which tells the browser to do something when the user clicks the element. This HTML code goes in the body of the HTML document.  

Here’s an example:

<div onclick="myClickFunction()"></div> 

The event listener points to a JavaScript function called myClickFunction. A function is a set of JavaScript code that enables you to pass data to the code and get data back from it.

Create the event handler 

Next, create a JavaScript function with the name you used for the onclick attribute. In this case, use myClickFunction. You define a function by using the keyword function followed by the function name. After the function name, add open and closing parentheses and then write the JavaScript code for the function between a pair of opening and closing curly braces. 

You can write this function inside a script element in the head or body elements of your HTML document or in a separate file.  

Here’s an example: 

function myClickFunction() { 
     alert(“This is a JavaScript message!”); 
} 
These are the basic steps for creating an event handler in JavaScript.

5.JavaScript mainly uses a programming model called functional programming. Functional programming uses functions to do work.
JavaScript can even send a function to another function.

6.Web developers create JavaScript functions to respond to events on a web page.
7.Constants hold data that can’t be changed.

8.Event listeners

They listen for a change to occur to an element. 

HTML elements can include event listener attributes that call JavaScript functions. 

An example of an event listener is an onclick attribute included in a button element.

9.Event handlers

They respond to, or handle, the event. 
Typically, they’re JavaScript functions that tell the browser how to respond. 

An example of an event handler is having a function that creates a pop-up success message when a user clicks a button.

10.
 */
